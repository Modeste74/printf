#ifndef _main_
#define _main_
#include <unistd.h>
#include <stdio.h>
#include <stdarg.h>
int _printf(const char *format, ...);
int print_char(char c);
void print_string(char *s);
void print_num(int foo);

#endif
